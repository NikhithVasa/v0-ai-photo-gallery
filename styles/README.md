# Styles

`styles/` is reserved for shared styling assets that do not belong in `app/globals.css` or component-local class names.

## Guidelines

- Keep global visual decisions in `app/globals.css` unless a separate stylesheet is clearly reusable.
- Avoid adding new global selectors for one-off component styling.
- Preserve the existing design system tokens and component conventions.
